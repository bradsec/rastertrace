# Image assets

`og-image-v2.png` is the active social preview referenced by `index.html`.
`og-image.svg` is retained as first-party branding source material from the
same release history. It is not a reproducible source for the current PNG and
must not be treated as generated output.
